pipeline {
    agent { docker { image 'node:6.3' } }
    stages {
        stage('build') {
            sh 'echo "Hello World"'
            steps {
                sh 'echo "Hello World"'
                sh 'npm --version'
                sh 'echo "Hello World"'
            }
        }
    }
}
